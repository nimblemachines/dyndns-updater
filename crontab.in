# Since the purpose of this (mostly) is to run the update-ip script, we
# don't want to get mail about commands that run.
MAILTO=""

# min hour mday month wday

# dyndns.org considers polling more often than every 10 minutes abusive.
# Polling more than every hour is probably overkill, anyway.
42 * * * *  $scriptdir/update-ip.sh
